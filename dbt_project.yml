name: "catalog"
version: "1.0.0"

profile: "catalog"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros", "shared/macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  catalog:
    +database: catalog
    main:
      raw:
        +materialized: table
      stg:
        +materialized: view
      mart:
        +materialized: table
